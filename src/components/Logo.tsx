import { View, Text } from "react-native";

const Logo = () => {
  return (
    <View className="w-full bg-transparent flex items-center justify-center">
      <Text className="font-bold text-white text-8xl shadow-md shadow-black">
        HUST
      </Text>
    </View>
  )
};

export default Logo;